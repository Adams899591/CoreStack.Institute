<?php

namespace Database\Factories;

use Illuminate\Database\Eloquent\Factories\Factory;

/**
 * @extends \Illuminate\Database\Eloquent\Factories\Factory<\App\Models\AcademicPeriod>
 */
class AcademicPeriodFactory extends Factory
{
    /**
     * Define the model's default state.
     *
     * @return array<string, mixed>
     */
    public function definition(): array
    {
        //    $baseSessionStart = 2025;
        // return [

             

                // for ($level = 100; $level <= 500; $level += 100) {
                //     $offset = ($level - 100) / 100;

                //     $startYear = $baseSessionStart - $offset;
                //     $endYear = $startYear + 1;

                //     $session = "{$startYear}/{$endYear}";

                //     foreach (['First', 'Second'] as $semester) {
                //         AcademicPeriod::create([
                //             'level' => $level,
                //             'session' => $session,
                //             'semester' => $semester,
                //         ]);
                //     }
                // }


        // ];
    }
}
